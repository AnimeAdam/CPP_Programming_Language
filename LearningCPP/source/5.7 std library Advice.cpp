#include <iostream> // basic input output cin cout

#include <vector> // for a container class
#include <valarray> // arithmetic operations for vectors
#include <list> // for a container class
#include <map> // for a container class
#include <unordered_map> // for a container class
#include <string> // it's a string
#include <regex> // for Regular Expressions
#include <forward_list> // Forward lists are sequence containers that allow constant time insert and erase operations
#include <iterator> // iterator through containers
#include <memory> // for manage dynamic memory

#include <random> // generates random numbers
#include <algorithm> // math functions
#include <complex> // for complex numbers 
#include <numeric> // operations on sequences of numeric values

#include <thread> // for thread templates
#include <mutex> // for mutexes
#include <future> // simple promise to do task in the future
#include <queue> // for queue container for context switching
#include <chrono> // for time stuff

#include <type_traits> //  obtain type information on compile-time

#include <cstddef> // implicitly generated or used by certain language expressions



using namespace std; // make std members visible
using namespace std::chrono; // see §35.2



int main()
{
	int q = 0;



	cin >> q; //just to stop at the command window
}